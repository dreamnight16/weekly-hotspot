# 编码规范

## 代码风格
- **简洁**：能用一句话解决的不用三句；不引入不必要的抽象、辅助函数、中间变量
- **高效**：优先时间复杂度与空间复杂度，避免无意义的拷贝、分配、遍历
- **可读性强**：命名精准、自文档化；结构清晰，一眼能看出意图
- **鲁棒性强**：不要信任任何外部输入；边界条件必须覆盖；静默失败不可接受

## 修改原则
- **先定位根源**：修改前必须理解问题的根本原因，不修表象
- **最小化修改**：用最小的改动解决问题，不改动无关代码，不做顺手重构
- **不引入回归**：修复一个问题时不能引入新问题

## 注释原则
- 默认不写注释，能通过命名和结构自解释的代码不需要注释
- 只在 WHY 不直观时写注释（隐藏约束、微妙的不变式、特定 bug 的 workaround）
- 禁止写 WHAT 注释（代码本身已经说明了做什么）
- 禁止写多行 docstring 或注释块，最多一行
